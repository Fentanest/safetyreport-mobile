# PROJECT_RULES.md — 제품 불변조건과 작업 규칙

모든 에이전트가 따른다. 공식 스킬·플러그인·일반 권고도 이 문서의 범위를 바꿀 권한이 없다.

## 1. 제품 불변조건
- 기존 신고 **조회·관리 앱**이다. 신고하기/신규 신고 접수, 일반 파일 업로드, 신고 레코드 삭제, 임의 시스템 공지, 새 글 작성 기능을 만들지 않는다.
  기존 DB 백업·복원, 로컬 파일 삭제/공유는 기존 기능이며 위 금지 항목과 다르다.
- 하단 5개 탭(대시보드/신고내역/신고관리/통계/알림 — 인덱스 0~4 고정). **2026-09-24 사용자 결정(D-06)** 으로 7개에서 줄였다:
  동기화·크롤링은 대시보드 상태 카드·앱바 버튼, 파일은 설정 > 데이터 관리에서 연다(`lib/navigation/app_routes.dart`).
  네이티브 딥링크의 옛 인덱스 5(파일)·6(동기화/크롤링)과 런처 바로가기(quick_sync/quick_crawl)는 이 화면들을 연다 — Kotlin 은 그대로 둔다.
- 신고내역 4개 분류(교통위반/주정차/기타위반/중복차량), 신고관리 4개 하위 탭(별점/감시 목록/중복 신고/데이터 수정),
  알림 3개 하위 탭(크롤링 현황/신고 결과/별점 주기)을 보존한다.
- Client의 크롤링과 Standalone의 동기화를 섞지 않는다. 진입점 라벨은 모드에 따라 `크롤링`/`동기화`다.
- 정부 정보 출처 링크(`https://www.safetyreport.go.kr/`)와 비공식 고지는 신고 상세 시트와 설정 > 앱 정보 두 곳에 유지한다.
- 서버 API/WS 계약(`server_contract.dart`, `ServerContract.kt`), 한국어 DB 컬럼, 로그인/보안 저장, 별점 제출, 중복 대표건, 백그라운드 동작(FGS/큐/WS)을 UI 미관 때문에 바꾸지 않는다.
- Provider → 다른 상태관리, Navigator → 새 라우터, Flutter → Compose/웹 재작성, 광범위 SDK/패키지/AGP 업그레이드는 별도 승인 없이 하지 않는다.

## 2. 정본(무엇이 기준인가)
- **기능 기준**: 사용자 명시 요구 + 코드로 확인한 현재 동작(`docs/design/feature-matrix.csv`) + 승인된 변경 사양(`docs/design/statistics-spec.md` 등).
- **시각 기준**: 사용자가 고른 짙은 네이비·블루 다크 컨셉과 대응 화이트 테마(`docs/design/ui-renewal-spec.md`). 이후 생성되는 보드가 이를 덮어쓰지 않는다.
- 시안 이미지 속 버튼·숫자·문구는 기능/API/집계의 증거가 아니다. PC 시안 구성은 모바일 내비게이션의 정본이 아니다.
- 이미지끼리 충돌하면 최신 이미지라는 이유로 고르지 않는다. 미결정 항목은 `ui-renewal-spec.md`의 "결정 필요"에 남긴다.

## 3. 권한과 금지
- 금지: `VERSION`·서명 파일·키스토어·실서버 데이터 변경, main 직접 push, 태그/릴리즈, 운영 로그인/별점 API로 테스트,
  개인 실기기 운영 앱 uninstall/`pm clear`, 실제 계정 데이터 변경, 골든 자동 갱신으로 실패 숨기기.
- 운영 데이터·개인정보·인증정보·실별점 결과를 산출물/레포에 넣지 않는다.
- `main.dart`, 테마, Provider, `pubspec.yaml`, Manifest, Gradle은 작업서에 지정된 한 담당자만 수정한다.
- 같은 파일·디바이스·실행 앱을 두 에이전트가 동시에 수정/조작하지 않는다. 위임 절차는 `docs/agent-dispatch-runbook.md`.

## 4. 완료 기준
- "빌드 성공"은 UI 완료가 아니다. 실제 Flutter 렌더(위젯/골든/앱 스크린샷)와 테스트 로그가 증거다.
- 보고는 **문서만 작성 / 도구만 연결 / 실제 테스트 통과**를 구분한다. 실행하지 않은 것은 NOT_RUN 또는 미검증으로 쓴다.
- 기존 `flutter analyze`/`flutter test` baseline(`docs/testing/ui-test-plan.md`)보다 나빠지지 않아야 한다. 기존 실패를 새 변경 탓으로 숨기거나 테스트를 지우지 않는다.
- 회귀 테스트의 고정 색상 단언을 바꿀 때는 원래 목적(선택/미선택 구분, 대비, 탭 이동)을 유지하는 단언으로 대체한다.

## DB 교환·변환 무결성 (최우선, 2026-09-24 사용자 지시)
- 서버↔모바일 DB 교환·변환에 **한 치의 오류나 누락도 없어야 한다.** UI·성능·일정 등 다른 목표와 충돌하면 이 규칙이 이긴다.
  대상 경로: 서버 `services/db_backup.py`(서버 DB 복원, 모바일 DB 판별·변환, 백업 다운로드), 모바일의 서버 DB 가져오기/내보내기·모드 전환 DB 이관.
- 테이블·컬럼·값 형식·의미를 바꾸는 변경은 **같은 작업 단위에서 양쪽 레포를 함께** 바꾼다: 양쪽 스키마, 양방향 변환, API 필드, 구버전 DB 업그레이드, 왕복 테스트.
  한쪽만 병합·배포하지 않는다.
- 왕복 테스트는 서버→모바일→서버, 모바일→서버→모바일 모두에서 **모든 컬럼 값이 같음**을 확인한다(NULL/빈 문자열 구분, 한글, 줄바꿈, 날짜·시각 형식, 정수/실수, 중복군·sync_meta 메타 포함).
- 변환 대상 컬럼 목록은 계약 문서 한 곳(각 레포 `docs/architecture/data-contracts.md`)에 두고, 양쪽 테스트가 그 목록과 실제 스키마가 일치하는지 검사한다.
- 모르는 컬럼·값을 조용히 버리지 않는다. 보존하거나, 명확한 오류로 중단한다.

## 확정 금액과 추정 금액의 분리 (2026-09-24 사용자 지시)
- 답변에 적힌 **확정** 과태료(`범칙금_과태료` 원문)와 규칙으로 계산한 **추정** 과태료·과징금은 데이터(별도 필드), 통계(별도 합계·열·계열),
  내보내기(별도 열), 화면 라벨에서 섞지 않는다. 추정값을 `범칙금_과태료`에 쓰지 않는다.
- 둘을 함께 보일 때는 "확정 N원 / 추정 M원(K건)"처럼 각각을 보이고, 합쳐진 한 숫자만 보이지 않는다. 과징금(밤샘주차)은 과태료와도 구분한다.
