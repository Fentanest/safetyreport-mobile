[작업 C — Flutter/Android 도구·테스트 적용 가능성 검토]
입력: .review-inputs/env.txt(로컬 환경), pubspec.yaml, pubspec.lock, android/ 설정(build.gradle.kts, settings.gradle.kts, AndroidManifest.xml, MainActivity.kt), test/ 전체, .github/workflows/build-apk.yml, build_*.sh, .review-inputs/analyze.log, .review-inputs/test.log.
목표: 이 앱의 UI 리뉴얼(테마·신고 카드·대시보드/통계 시범 구현)에 대해 다음 도구의 적용 가능성·전제조건·위험을 검토한다: flutter_test widget test + 접근성 Guideline API(androidTapTargetGuideline, textContrastGuideline, labeledTapTargetGuideline), matchesGoldenFile 골든(폰트/플랫폼 차이), integration_test, Patrol(권한·알림·파일선택 native UI), Dart/Flutter MCP(dart mcp-server), Flutter agent-plugins/Dart skills, Android emulator/adb, Android CLI.
규칙: 네트워크 조회가 불가하므로 공식 문서 내용은 "지식 기반(미검증)"으로 표시하고, 레포/환경에서 확인한 사실과 구분한다.
반드시 다룰 것: (1) 현재 test/의 테스트 종류와 무엇을 회귀 방지하는지(파일:줄), 특히 test/report_navigation_regression_test.dart 의 TabBar 색상 단언(Colors.white 등)이 새 테마에서 어떻게 바뀌어야 본래 목적(선택/미선택 구분·대비·탭 이동)을 유지하는지; (2) sqflite/SharedPreferences/MethodChannel/flutter_secure_storage/video_player/flutter_map/cached_network_image 가 widget/golden 테스트에서 필요로 하는 mock·fake 전략; (3) Client/Standalone 각각의 fixture 주입 지점(ReportProvider/ApiService/LocalDbService 등 코드 경로:줄); (4) 골든 테스트용 한글 폰트 로딩 필요성(기본 Ahem 폰트 문제); (5) integration_test/Patrol 도입 시 android/ 에 필요한 변경과 production entrypoint에 넣으면 안 되는 것; (6) 현재 환경(env.txt)에서 즉시 가능한 것 / 설치가 필요한 것 / 막힌 것.
반환 형식: "## 도구별 판정" 표(도구 | 적용 가능 | 전제조건 | 위험 | 근거 | 권고 순위), "## 기존 테스트 분석", "## mock/fixture 전략", "## 환경 blocker", "## 실행 증거".
