[작업서 공통 — 범위 밖 화면 디자인 통일 / Opus 5.5 발행 / 상향 권한(사용자 승인)]
- 레포 safetyreport-mobile (Flutter, Provider). base SHA 00028685807af0cfd1ffc58149966aae9294721b. 워크스페이스 = 너 전용 detached worktree(아래 경로). 폐기용 사본이며 Opus 가 네 diff 를 검수 후 메인에 반영한다.
- 먼저 읽기: AGENTS.md, PROJECT_RULES.md, docs/design/ui-renewal-spec.md, docs/architecture/overview.md 의 "2026-09-24 이후 변경", lib/theme/sr_colors.dart, lib/theme/app_theme.dart, lib/widgets/status_badge.dart, lib/widgets/report_list_card.dart(이미 통일된 예시), lib/screens/dashboard_screen.dart(이미 통일된 예시).
- 목표: 담당 파일의 하드코딩 색(Colors.*, Color(0x..), shade 값, withOpacity)을 테마 토큰으로 바꿔 라이트/다크(슬레이트) 모두 시안 톤과 가독성을 맞춘다.
  - 글자/배경/테두리: context.sr.textPrimary/textSecondary/textDisabled/background/surface/surfaceAlt/border, Theme.of(context).colorScheme.primary/onPrimary/error 등.
  - 처리상태·처분 배지: StatusBadge / StatusBadge.status(status). 상태 기준색으로 글자·아이콘을 칠해야 하면 StatusTone.of(color, brightness:, surface:).foreground 를 쓴다(기준색 그대로 글자에 쓰지 말 것: AA 미달).
  - 경고/오류/안내 박스: StatusTone 의 background/border/foreground 조합.
  - withOpacity → withValues(alpha:) 로.
- 절대 바꾸지 말 것: 데이터 흐름, 콜백, 표시 필드/문구의 의미, 네비게이션, 모드 분기(Client 크롤링 / Standalone 동기화 / 데모), 서비스·Provider·모델, pubspec, android/, lib/theme/*, lib/widgets/status_badge.dart·mode_badge.dart·sr_tab_bar.dart·report_list_card.dart, 담당 외 파일. 새 기능 추가 금지(신고하기, 업로드, 삭제 등).
- 예외 규칙: 크롤링/동기화 로그 패널은 라이트/다크 모두 어두운 터미널 배경 유지(사용자 결정). 지도 타일/마커 색 로직(_mapPointColorForFineRate 등)은 기능이라 그대로, 지도 화면은 주변 UI(앱바·카드·시트·칩)만 통일.
- 허용: 셸, 이 worktree 안 파일 수정, flutter analyze / flutter test / dart format. 금지: worktree 밖 쓰기(특히 /home/better0101/projects/safetyreport-mobile), git commit/push/stash, 전역 설치, 에뮬레이터/adb, 네트워크 API 호출, --update-goldens.
- 완료 조건: (1) 담당 파일에 Colors./Color(0x 하드코딩이 남으면 줄마다 이유를 응답에 적는다(예: 터미널 로그 패널), (2) flutter analyze 에서 error/warning 증가 없음, (3) flutter test 전체 통과, (4) dart format 적용.
- 반환: "## 변경 요약"(파일별 무엇을 바꿨는지), "## 남긴 하드코딩과 이유", "## 동작 보존 확인"(각 파일에서 콜백·필드가 그대로임을 어떻게 확인했는지), "## 실행 증거"(명령+종료코드+마지막 줄, git status --short, git diff --stat).
